# 必需
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxx

# 可选：strict function schema 需要 /beta 端点，默认已指向它
DEEPSEEK_BASE_URL=https://api.deepseek.com/beta

# 可选：本地 DeepSeek tokenizer 路径，用于精确的 chunk 预算
# 不设置则退化为字符数估算
DEEPSEEK_TOKENIZER_PATH=

# 可选：写入报告尾部，便于把一条评论追溯到具体构建
COMMIT_HASH=
